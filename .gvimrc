" Use the solarized colour scheme
colorscheme solarized

" Set guifont to deja vu sans mono at 11pt
set guifont=DejaVu\ Sans\ Mono:h11

" Set no. of colours to 256
set t_Co=256

" Set a gui tab label
" The format is the same as in statusline
" So here, the modified flag, then filename
set guitablabel=%M\ %t

" I like dark bgs
set background=dark

" Set the spacing between lines
set linespace=2

" Show the line the cursor is on in a different highlight
set cursorline

" Set guioptions to be
" e -> GUI tabs
" g -> make menu items not used, gray
" m -> Menubar is present
set go=egm
